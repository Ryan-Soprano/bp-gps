<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/list_item_spacing"
    app:cardCornerRadius="8dp"
    app:cardElevation="2dp"
    app:cardUseCompatPadding="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="@dimen/list_item_padding"
        android:gravity="center_vertical"
        android:background="?android:attr/selectableItemBackground">

        <TextView
            android:layout_width="@dimen/icon_background_size"
            android:layout_height="@dimen/icon_background_size"
            android:layout_marginEnd="@dimen/list_icon_margin"
            android:background="@color/police_blue"
            android:text="📍"
            android:textColor="@color/white"
            android:gravity="center"
            android:textSize="16sp" />

        <!-- Address Content -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tvAddress"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textAppearance="@style/TextAppearance.App.ListItemAddress"
                android:maxLines="2"
                android:ellipsize="end"
                tools:text="115 Jefferson St, Lafayette, LA" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginTop="4dp"
                android:gravity="center_vertical">

                <TextView
                    android:id="@+id/tvTime"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textAppearance="@style/TextAppearance.App.ListItemDetail"
                    android:gravity="center_vertical"
                    tools:text="2:94 PM" />

                <TextView
                    android:id="@+id/tvOfficer"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textAppearance="@style/TextAppearance.App.ListItemDetail"
                    android:textColor="@color/colorError"
                    android:textStyle="bold"
                    android:layout_marginStart="12dp"
                    android:visibility="gone"
                    tools:text="• Officer 111"
                    tools:visibility="visible" />
            </LinearLayout>
        </LinearLayout>

        <!-- Status Indicator -->
        <View
            android:id="@+id/statusIndicator"
            android:layout_width="4dp"
            android:layout_height="@dimen/status_indicator_height"
            android:background="@color/badge_gold"
            android:layout_marginStart="8dp" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>