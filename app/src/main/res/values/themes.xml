<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.BpGps" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary colors -->
        <item name="colorPrimary">@color/police_blue</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary colors -->
        <item name="colorSecondary">@color/badge_gold</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Background colors -->
        <item name="android:colorBackground">@color/colorBackground</item>
        <item name="colorSurface">@color/colorSurface</item>
        <item name="colorOnBackground">@color/colorOnBackground</item>
        <item name="colorOnSurface">@color/colorOnSurface</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">@color/police_blue_dark</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Default button style -->
        <item name="materialButtonStyle">@style/Button.App</item>
    </style>

</resources>